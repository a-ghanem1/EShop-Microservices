{{- $name := printf "%s-catalog-api" .Release.Name -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $name }}-secret
  labels:
    app: catalog-api
    release: {{ .Release.Name }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.secret.dbUser }}"
  DB_PASSWORD: "{{ .Values.secret.dbPassword }}"
